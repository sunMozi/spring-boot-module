<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mozisun.springboottlias.mapper.EmpMapper">

  <select id="selectEmpWithDept"
          resultType="com.mozisun.springboottlias.model.Dos.EmpListDo">
    SELECT *
    FROM emp
    <where>
      <if test="name != null and name != ''">
        name like CONCAT('%', #{name}, '%')
      </if>
      <if test="gender != null">
        and gender = #{gender}
      </if>
      <if test="begin != null and end != null">
        AND entry_date BETWEEN #{begin} and #{end}
      </if>
    </where>
  </select>
</mapper>