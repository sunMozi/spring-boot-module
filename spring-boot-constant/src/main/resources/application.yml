spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: root
    password: password

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.constantdemo.model